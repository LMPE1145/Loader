-- 检查RULER变量是否存在
if RULER == nil then
    -- 发送原生通知：未同意协议
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "⚠️ 协议检查失败",
        Text = "未同意协议，请设置RULER=\"YES\"",
        Icon = "rbxassetid://6881975538", -- 警告图标
        Duration = 5
    })
    
    -- 控制台输出
    warn("❌ 未同意协议，请设置RULER=\"YES\"")
    return -- 停止执行脚本
end

-- 检查RULER是否为"YES"
if RULER ~= "YES" then
    -- 发送原生通知：格式错误
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "❌ 协议格式错误",
        Text = "RULER必须设置为\"YES\"（大写）",
        Icon = "rbxassetid://6881975538",
        Duration = 5
    })
    
    -- 控制台输出
    warn("❌ 协议格式错误，RULER必须设置为\"YES\"")
    return -- 停止执行脚本
end

-- 协议检查通过
-- 发送原生通知：协议通过
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "✅ 协议检查通过",
    Text = "已同意协议，准备加载脚本...",
    Icon = "rbxassetid://6881979636", -- 成功图标
    Duration = 2
})

-- 控制台输出
print("✅ 协议检查通过")
print("📜 用户已同意使用协议")

-- 延迟显示加载提示
task.wait(1)

-- 发送原生通知：正在加载脚本
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "📃↹📂 正在加载脚本",
    Text = "请稍候，脚本正在加载中...",
    Icon = "rbxassetid://6881972516", -- 加载图标
    Duration = 3
})

-- 控制台输出
print(" 正在加载脚本...")
print("📃↹📂 脚本加载中，请稍候")

-- 延迟显示加载动画
local dots = {".", "..", "...", "...."}
for i = 1, 10 do
    local dot = dots[(i % #dots) + 1]
    print("📂 加载中" .. dot)
    task.wait(0.1)
end

-- 执行主脚本
print("执行主脚本...")
loadstring(game:HttpGet("https://raw.githubusercontent.com/LMPE1145/Loader/refs/heads/main/LMPECENTER.lua"))()
